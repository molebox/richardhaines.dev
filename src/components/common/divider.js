/** @jsx jsx */
import { jsx } from "theme-ui";

const Divider = () => {
  return (
    <div
      sx={{
        border: "solid 1px",
        color: "accent",
        width: "100%"
      }}
    ></div>
  );
};

export default Divider;
